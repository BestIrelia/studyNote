vue单页面应用，在iOS上，点击微信自带返回按钮会出现白屏效果，滑动页面才会消失。
解决方法：在获取数据后加入下面代码，如果获取数据带分页，则在分页页码为1时执行下面代码
                        this.$nextTick(() => {
                            window.scrollTo(0, 1);
                            window.scrollTo(0, 0);
                        });