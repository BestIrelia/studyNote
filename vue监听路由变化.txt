一、在App.vue里面监听所有路由（所有路由均为子路由）变化
	watch: {
			$route: function() {

				// 路由变化时执行函数
				
			}
		}

二、在父组件vue文件监听父子组件路由来回切换时路由变化（父子关系看路由文件的children。
在views下文件夹下的index.vue文件可以监听其所有子路由变化）
方法一：同上
方法二：
 watch: {
            // 如果路由有变化，会再次执行该方法
            "$route": "fetchDate"
        },
 method: {
            	//路由变化
			fetchDate(){
			   
				
			}
        },